{
  "name": "hasizme_corporate",
  "version": "1.0.0",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "frontend:build": "cd frontend && npm run build",
    "backend:dev": "cd backend && wrangler dev",
    "backend:deploy": "cd backend && wrangler publish"
  },
  "devDependencies": {
    "wrangler": "^3.78.12"
  }
}
